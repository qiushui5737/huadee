# =====================================================
# 个人本地配置（不提交到仓库，已在 .gitignore 中排除）
# 每位开发者根据自己的本地环境修改此文件
# =====================================================

spring:
  datasource:
    username: root
    password: root
  data:
    redis:
      password:
